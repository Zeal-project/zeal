
<h1>
  Create <%= current_user.email %> Resume
</h1>

<div class="container">
  <%= form_for @resume, :url => user_resumes_path do |f| %>
    
    <%= render :partial => 'resume_template', :locals => { f: f } %>

    <%= f.submit "Create", :class => "btn btn-primary" %>
    <%= link_to "Cancel", my_resume_user_resumes_path(current_user), :class => "btn btn-default" %>
  <% end %>
</div>


